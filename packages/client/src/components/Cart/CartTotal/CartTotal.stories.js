import React from 'react';

import CartTotal from './CartTotal.component';

export default {
  title: 'Cart',
  component: CartTotal,
};

const Template = () => <CartTotal />;

export const Total = Template.bind({});
